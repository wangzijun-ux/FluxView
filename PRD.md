# FluxView 製品要求仕様書

更新日: 2026-03-20  
対象版: 現行プロトタイプ / 管理画面・作業者画面統合版

## 1. 文書の目的

本書は、物流現場向けオペレーション可視化システム「FluxView」の目的、対象業務、画面構成、機能要件、データ要件、非機能要件、および受け入れ条件を整理した製品要求仕様書である。  
本書の目的は以下の通りである。

- 開発者、企画担当者、レビュー担当者の間で、現在の実装方針と今後の拡張方針を共有する
- 実装済み機能と未実装機能を明確にし、優先度の判断材料を揃える
- 物流現場で必要な判断、操作、記録、分析をどの単位で扱うかを定義する
- 将来的な外部連携、永続化、権限制御に向けた前提条件を固定する

本PRDは、理想論だけを記述する文書ではなく、現行リポジトリの実装内容と整合することを重視する。

## 2. 製品概要

FluxView は、物流拠点における日々の運営を、管理者と作業者の双方から可視化・調整・記録・分析するためのWebアプリケーションである。  
管理者は、当日の拠点状況、工程進捗、人員配置、通知、送信実績、原価差異を一つのシステム上で確認・操作できる。  
作業者は、当日担当する工程、開始・中断・完了、数量入力、通知確認をモバイルUIから実施できる。

現時点での主な特徴は以下の通りである。

- 拠点単位で現場状況を把握できる
- ワークフローと工程単位で作業順と進捗を定義できる
- 現場配置を時間帯ベースで可視化できる
- 作業者の数量入力と実績送信を簡潔なUIで扱える
- 派遣会社や雇用区分を含めたコスト分析が可能である
- ローカルストレージを中心とした軽量なデモ運用が可能である

## 3. 背景と課題

物流現場では、以下のような課題が繰り返し発生する。

- 管理者が、当日の遅延や要員不足を複数の表や口頭連絡から把握している
- 作業計画、実績、勤怠、派遣情報、コスト情報が分断されている
- 作業者の実績入力が遅い、または粒度が不揃いで、当日判断に使いづらい
- 拠点内のどの工程が遅れているか、どの作業に何人必要かを即時に判断しづらい
- 派遣比率の変化や稼働効率の低下が、原価差異として見えにくい

旧設計では「エリア」概念を持っていたが、運用上の正式な管理単位である「拠点」と責務が重複し、以下の問題を引き起こしていた。

- マスタ構造が複雑になり、画面ごとに異なる粒度で表示される
- ワークフロー作成時に不要な選択項目が増える
- 集計軸が増え、ユーザーがどの単位で見ているか分かりにくくなる
- 実装上も `siteId` と `areaId` の関係が曖昧になりやすい

このため、本版ではシステム上の正式な概念としての「エリア」を廃止し、管理・表示・保存の主軸を「拠点」に統一する。

## 4. 製品ビジョン

FluxView のビジョンは、物流現場の判断に必要な情報を、現場責任者と作業者の双方にとって迷いなく扱える形で統合することである。  
単なる画面集ではなく、日々の運営判断に必要な「今どこで何が起きているか」「どこを動かせば遅延を抑えられるか」「その結果コストがどう変わるか」を、一連の流れで見られることを目指す。

## 5. 管理単位の定義

本システムで扱う正式な管理単位は以下とする。

- 荷主
- 拠点
- 工程
- ワークフロー
- 作業者
- 派遣会社
- 資格
- スキル

補足:

- 「エリア」は正式なマスタとして扱わない
- 旧データに `areaId` が存在する場合のみ、互換目的で `siteId` として読み替える
- 新規保存、新規設計、新規画面では `areaId` を前提にしない

## 6. 対象ユーザー

### 6.1 現場責任者

役割:

- 当日の拠点運営を管理する
- 工程ごとの進捗や遅延を監視する
- 人員配置を調整する
- 応援や移動指示を通知する

主なニーズ:

- 拠点全体の状況を即座に把握したい
- 遅延している工程を特定したい
- どの工程に何人必要かを把握したい
- 当日変更を作業者へ素早く共有したい

### 6.2 業務管理者

役割:

- マスタやワークフローを整備する
- 原価や計画差異を分析する
- 派遣会社や雇用構成を管理する

主なニーズ:

- 工程定義や必要条件を整えたい
- 拠点別の運用構成を維持したい
- 計画値と実績値の差異を可視化したい
- 派遣比率とコストの関係を見たい

### 6.3 作業者

役割:

- 当日の担当作業を確認する
- 作業開始、中断、完了を記録する
- 数量を入力する
- 通知を確認する

主なニーズ:

- 今やるべき作業を迷わず理解したい
- 数量入力を最小限の操作で済ませたい
- 移動指示や注意事項を見落としたくない

## 7. スコープ

### 7.1 本版の対象

- ダッシュボード
- 現場配置
- 作業可視化
- 送信実績
- 進捗管理
- コスト分析
- 派遣管理
- ワークフロー管理
- マスタ管理
- 通知管理
- 作業者モバイル画面
- 作業者バンドビュー

### 7.2 本版の対象外

- 正式なサーバーサイド認証基盤の完成
- 本番向け権限制御の厳密実装
- すべての画面に対するサーバー同期
- 旧デモ画面の全面リニューアル
- 帳票出力の完成版
- AI による最適配置の本番実装

## 8. 画面一覧

現行プロトタイプで提供する主要画面は以下である。

- ログイン
- ダッシュボード
- 現場配置
- 進捗管理
- 作業可視化
- 送信実績
- コスト分析
- 派遣管理
- ワークフロー管理
- マスタ管理
- 通知管理
- ユーザー管理
- 設定
- 作業者ビュー
- 作業者バンドビュー

## 9. 機能要件

### 9.1 ダッシュボード

目的:

- 拠点全体の稼働状況を俯瞰する

要件:

- 拠点を基準に当日のワークフロー状況を表示できること
- KPI カードで、進捗、稼働数、遅延件数などを視認できること
- 時間推移チャートで予定と実績を比較できること
- 荷主別、ワークフロー別に進捗状況を確認できること
- アラート一覧で遅延や注意点を表示できること
- 表示にエリア概念を用いず、拠点名を中心に表現すること

### 9.2 現場配置

目的:

- 当日の工程配置を時間帯ごとに把握し、必要に応じて再配置する

要件:

- 拠点配下のワークフローと工程を一覧できること
- 時間帯別の配置スナップショットを扱えること
- 荷主、拠点、工程、キーワードで絞り込みできること
- 配置変更を保存できること
- 変更内容から通知候補を生成できること
- 工程ごとに必要スキルや資格の不足を表現できること
- 画面上の名称やフィルタでエリアを出さないこと

### 9.3 進捗管理

目的:

- 計画数、実績数、遅延、完了見込みを管理者が把握できるようにする

要件:

- ワークフロー別、工程別に進捗を確認できること
- 拠点単位で集計できること
- 予定、実績、残数、進捗率、遅延数、ETA を表示できること
- 進捗トレンドを表示できること
- フィルタに荷主、拠点、工程を使用できること

### 9.4 作業可視化

目的:

- 工程別と作業者別の両軸で、当日の動きを見られるようにする

要件:

- ワークフロー表示と作業者表示を切り替えられること
- 時間粒度を切り替えられること
- 作業者ごとの担当工程と時間帯を確認できること
- 工程ごとの並び順を視認できること
- 画面内の表示単位は拠点中心であること

### 9.5 送信実績

目的:

- 作業者が送信した数量実績を、管理者が当日中に確認できるようにする

要件:

- 日付、荷主、拠点、工程、作業者、状態で絞り込みできること
- サマリー表示とログ表示を切り替えられること
- 工程別に送信件数、数量、作業者数、中断時間、最終送信時刻を確認できること
- 作業者別ログで開始予定、終了予定、最新更新、数量、状態を確認できること
- 集計キーとしてエリアを使わないこと

### 9.6 コスト分析

目的:

- 原価と生産性の関係を拠点視点で分析する

要件:

- 雇用区分別に工数と原価を集計できること
- 荷主別または工程別に分析軸を切り替えられること
- 予定数量と実績数量、予定原価と実績原価を比較できること
- 派遣会社単価を加味した分析ができること
- エリア別原価集計は行わないこと

### 9.7 ワークフロー管理

目的:

- 拠点単位の標準作業フローを定義する

要件:

- ワークフローは荷主と拠点の組み合わせで作成できること
- 工程を順序付きで追加、削除、並び替えできること
- 各工程に必要資格、必要スキル、標準人数、標準 UPH を設定できること
- ワークフロー一覧から詳細を編集できること
- 新規作成時にエリア選択を要求しないこと
- 自動命名は「荷主_拠点」を基準とすること

### 9.8 マスタ管理

目的:

- 業務運用に必要な基礎情報を管理する

対象マスタ:

- 荷主
- 拠点
- 資格
- スキル
- 派遣会社
- 工程

要件:

- 一覧表示、追加、更新、削除ができること
- 工程に必要資格と必要スキルを紐づけられること
- エリアマスタの追加・編集・削除 UI を持たないこと

### 9.9 通知管理

目的:

- 管理者から作業者、または拠点単位に情報を伝達する

要件:

- お知らせ、注意喚起、移動指示などを作成できること
- 配信先を全体、拠点、対象作業者などで切り替えられること
- 送信済み、予約、既読状況を確認できること
- 作業者画面に適切な形で表示されること

### 9.10 作業者画面

目的:

- 作業者が当日の担当作業を素早く理解し、実績入力を行えるようにする

要件:

- ログイン後に当日の担当作業一覧を表示できること
- 担当工程、開始時刻、終了時刻、予定数量を表示できること
- 作業開始、中断、完了を操作できること
- 数量をボタンとテンキーで入力できること
- 通知とアナウンスを確認できること
- 画面上は拠点と工程で情報を構成すること

### 9.11 作業者バンドビュー

目的:

- 作業者向けに、さらに簡潔な表示で当日情報を提示する

要件:

- 次作業、移動先、目標数量、通知をコンパクトに表示できること
- 操作は最小限のタップ数で成立すること
- 表示文言は現行の拠点基準に寄せること

## 10. データ要件

### 10.1 主要エンティティ

- `Shipper`
- `Site`
- `Qualification`
- `Skill`
- `DispatchCompany`
- `Process`
- `WorkflowDefinition`
- `WorkflowStepSetting`
- `WorkerTaskRecord`
- `WorkerSubmissionRecord`
- `WorkerNotificationRecord`

### 10.2 永続化方針

現行プロトタイプでは、主にローカルストレージを用いて以下を保存する。

- マスタデータ
- 選択中拠点
- 配置スナップショット
- 作業者の進捗状態
- 送信実績の元データ
- 通知
- テーマ設定

### 10.3 ワークフローモデル

- `WorkflowDefinition` は `shipperId` と `siteId` を保持する
- 工程順は `steps` で保持する
- 工程ごとに必要資格、必要スキル、標準人数、UPH を保持する
- 新規保存時に `areaId` を正式な属性として保存しない

### 10.4 互換方針

- 旧保存データに `areaId` が存在する場合、読み込み時に `siteId` として正規化する
- 旧画面互換のため、一部コンポーネントに互換値を返すことは許容する
- ただし、新規要件、新規画面、新規保存設計ではエリア概念を使わない

## 11. 外部連携方針

### 11.1 想定連携先

- WMS
- TMS
- 勤怠システム
- 派遣管理システム
- 会計システム

### 11.2 連携対象の例

- 入荷・出荷予定数量
- SKU 属性
- 配送計画
- 出勤、休憩、退勤
- 派遣単価
- 原価データ

### 11.3 現時点の前提

- 現行プロトタイプでは外部連携は本番実装されていない
- 将来的に API またはファイル連携へ拡張できる構造を前提とする
- FluxView は現場運用と可視化の統合レイヤーとして位置づける

## 12. 非機能要件

### 12.1 性能

- 管理画面は日常業務でストレスなく操作できること
- グラフ、表、カード表示が実用的な速度で描画されること
- 作業者画面は短時間で起動し、操作応答が軽いこと

### 12.2 可用性

- 現場ネットワークが不安定でも、直前までの操作を保持できること
- 主要な入力や配置調整がブラウザ内で完結できること

### 12.3 保守性

- 管理単位を拠点に統一し、冗長な概念を減らすこと
- 画面間で同じ概念に同じ名称を使うこと
- 将来的な Supabase 連携へ移行しやすい構成を保つこと

### 12.4 セキュリティ

- 本番では認証、認可、監査ログが必要である
- 現行はデモ用途のため簡易認証を含むが、本番要件とは分離して扱う

### 12.5 オフライン耐性

- Wi-Fi が不安定な現場を想定し、作業実績は一時保存できること
- 復帰後も表示・集計が破綻しないこと

## 13. UI / UX 方針

### 13.1 管理者画面

- 情報量は多くても、判断に必要な優先度が分かること
- グラフ、表、カードを組み合わせて複数視点を一画面で扱うこと
- 色は注意喚起や状態表現に一貫して使うこと

### 13.2 作業者画面

- 迷わない操作順序であること
- 数量入力を高速に行えること
- 通知や移動指示を見落としにくいこと

### 13.3 用語統一

- 拠点
- 荷主
- 工程
- ワークフロー
- 作業者
- 派遣会社

補足:

- 新規 UI で「エリア」を主要ラベルとして露出させない

## 14. 受け入れ条件

以下を満たした場合、本版の主要要件を満たしていると判断する。

- 主要画面にエリア管理タブが表示されない
- ワークフロー作成・編集が荷主と拠点のみで成立する
- ダッシュボード、現場配置、進捗管理、送信実績、コスト分析が拠点基準で成立する
- 作業者画面が拠点・工程中心の情報で動作する
- 旧データ読込時に画面が破綻しない
- ビルドが成功する

## 15. 今後の拡張候補

- 本番認証基盤の整備
- Supabase を用いた永続化の正式導入
- ロールごとの厳密な権限制御
- 通知配信履歴の強化
- CSV / PDF 出力
- 外部連携の本実装
- AI を用いた人員最適配置支援
- 旧デモ画面の整理と統合

## 16. 補足事項

- 本版は、実装中のプロトタイプを基準に整理したものである
- 旧設計の名残として一部内部実装に互換コードが残る可能性がある
- ただし製品要件としては、管理単位を拠点に統一し、エリア概念を廃止する方針を正式採用する

UIを日本語に統一
１．ワークフロー管理を業務管理に変える
２．拠点Topメニューで選択し，業務管理で新規業務を作成する時、再び選択しないように。他の画面も同じく再び選択しないように。
３．業務管理で工程を設定する時、資格、スキルと別で該当工程のマニュアル・注意事項を記載できるように
４．現場配置は荷主に分ける必要がありません。工程別で分けます。
５．進捗管理は業務ー工程別で、予定数入力、進捗見るように
６．送信実績画面は工程別サマリーする必要がありません。
７．送信実績画面、送信ログ別で管理者がランク付け、コメントできるように
８．現場配置する際に、工程に求められてるスキル・資格に対して、配置しようとした作業員が満たさない場合、アラートで出ます。配置自体は制限しません。